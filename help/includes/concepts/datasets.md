<!-- CANONICAL SUMMARY -->
A **dataset** is a collection of OME-Zarr images inside a project, together 
with their processing history. Every image in your dataset can have attributes 
and types. Each workflow you run reads from and writes back to a dataset to 
add new images or update their metadata.
