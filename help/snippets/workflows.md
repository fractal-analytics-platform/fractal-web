<!-- CANONICAL SUMMARY -->

A **workflow** is an ordered sequence of tasks that processes the images in a
dataset — for example converting to OME-Zarr, segmenting objects, then measuring
features. You build a workflow by adding and arranging tasks, then run it on a
dataset to produce results.
